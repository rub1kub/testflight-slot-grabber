[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "testflight-slot-grabber"
version = "0.1.0"
description = "Conservative TestFlight public-slot monitor and macOS Accessibility automation"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["testflight_grabber"]
